# Install public DSH peers so a clean clone and the tag publisher can build
# without relying on local symlinks into a DeepSeek Harness source checkout.
auto-install-peers=true
strict-peer-dependencies=false
