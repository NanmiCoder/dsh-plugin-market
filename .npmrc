# @deepseek-ai/* are provided by the DSH runtime environment: never
# auto-install peers, and never fail the install on an unmet one. Build-time
# type/value resolution comes from the symlinks documented in README.md.
auto-install-peers=false
strict-peer-dependencies=false
